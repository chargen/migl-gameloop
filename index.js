"use strict";

module.exports = {
    flexible: require('./src/flexible'),
    fixedFullFramerate: require('./src/fixed-full-framerate'),
    fixedHalfFramerate: require('./src/fixed-half-framerate')
};
